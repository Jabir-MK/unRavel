import 'package:flutter/material.dart';

// BorderRadius with fixed values

BorderRadius jSRadius = BorderRadius.circular(8);
BorderRadius jMRadius = BorderRadius.circular(12);
BorderRadius jLRadius = BorderRadius.circular(15);
BorderRadius jXLRadius = BorderRadius.circular(50);

// Padding with fixed values

const jDefaultPadding = 10.0;
